let Hours = [5, 6, 7, 3, 10];
let EmpRate = [600, 300, 400, 500, 700];
let Pay = 0;
/*
console.log(Hours, EmpRate);
console.log(Pay);
for(let i of Hours)
{
	for (let j of EmpRate)
		*/
	for (let i=0; i<Hours.length; i++)
	Pay = Hours[i] * EmpRate[i]
	{
		console.log(Pay);
	}