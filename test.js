let x1 = 6;
let x2 = 8;
let count = (x1>x2)
{
	if (x1%x2==LCM)
	{
	console.log(LCM);
	break;
	}
}
if (LCM>10) {
for (y=8; y<=12; y++)
	console.log(LCM + "*" + y + "=" + LCM*y);
else if (LCM<10)
	console.log(LCM);
}